services:
  pixhub:
    build: .
    container_name: pixhub
    ports:
      - "${PORT}:3000"
    volumes:
      - "${DATA_DIR}:/app/data"
      - "${RESOURCES_DIR}:/app/resources"
    restart: unless-stopped
