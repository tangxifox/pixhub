services:
  pixhub:
    build: .
    container_name: pixhub
    ports:
      - "${DOCKER_PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
      - "${DOCKER_RESOURCES:-./resources}:/app/resources"
      - ./.env:/app/.env:ro
    restart: unless-stopped