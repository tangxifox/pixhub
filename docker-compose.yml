version: '3.8'

services:
  pixhub:
    build: .
    container_name: pixhub
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
      - "${RESOURCES:-./resources}:/app/resources"
      - ./.env:/app/.env
    restart: unless-stopped